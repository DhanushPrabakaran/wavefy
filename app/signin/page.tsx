import React from "react";
import SignIn from "../_components/sign-in";
// import { auth } from "@/auth";
const page = () => {
  // const session =
  return (
    <div>
      <SignIn />
    </div>
  );
};

export default page;
